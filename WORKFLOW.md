# WORKFLOW.md — 对话循环行为策略

> 活文档：定义 Agent 在统一对话循环中的行为策略。改行为先改本文档，再同步 prompt。
> 最后更新：2026-09-05　版本：v0.2（新增 §5 通用操作策略，原 §5~§10 顺延）

## 1. 循环总览

单 Agent 统一对话循环：用户消息 → 模型逐轮决策 → 动作执行 → 观察 → 再决策，
直到模型用话语权动作（reply / ask_user / plan）把话语权交还用户。

**不预设"操作型/认知型"分类**。模型每轮自主决定行为策略：

- 不需要操作浏览器就能回答 → 直接 `reply`
- 意图不清 → `ask_user` 追问
- 复杂任务 → 先 `plan` 输出纲要再执行
- 简单明确 → 直接执行，完成后 `reply` 汇报

## 2. 每轮输出契约

```json
{"thought": "对当前状态的分析和下一步打算，一两句话",
 "memory": "跨轮工作记忆，≤500字，每轮重写；不需要时省略此字段",
 "action": {"type": "...", ...}}
```

- `thought`：面向用户的思考流，UI 流式展示
- `memory`：跨轮记忆便签。读到关键信息立即蒸馏记入；它会在后续每轮注入。
  认知型任务靠它跨页面累积要点；操作型任务通常不需要
- `action`：每轮恰好一个动作

## 3. 话语权动作（结束当前回合，等待用户）

| 动作 | 用途 |
|---|---|
| `reply(text)` | 对用户说话：任务完成汇报 / 回答问题 / 说明进展与卡点。text 支持 markdown |
| `ask_user(question)` | 追问。对话挂起等待回答，**无超时**——用户何时回答都继续 |
| `plan(steps)` | 输出执行纲要（格式见 §7），**不等待确认，随后直接执行**；用户可随时叫停 |

任务完成不用 `done`，用 `reply` 汇报——操作汇报与认知报告统一。

## 4. 何时追问（ask_user）

**问**：
- 目标对象有歧义，且无法从对话历史、标签页快照、页面内容推断
- 多个候选且选错代价高
- 缺少完成任务必需的信息（如"发给谁""搜什么"）
- 想改用非浏览器手段（API、命令行等）时，必须先请示（见 §5.4）

**不问**：
- 能从上下文推断的；有合理默认值的选择（如排序方式、看第几个结果）
- 能自己 new_tab/goto 解决的冷启动问题

**纪律**：同一回合最多追问一次；用户回答后必须推进，不得就同一问题反复问。
**不可逆动作（发布、删除、支付、关注、发送）执行前必须 ask_user 确认，无例外。**

## 5. 通用操作策略（模拟真实用户优先）

### 5.1 默认模拟真实用户
搜索、导航等操作默认走页面本身的路径：`input_text` 输入关键词 → 点击搜索按钮 /
点击链接进入。这是跨站点最鲁棒的方式。

### 5.2 goto/new_tab 的使用边界
只打开**确定可靠、稳定**的 URL：官网首页、站点知识包明确给出的直链、已验证的
规范 URL。**不要**自行构造搜索/查询参数 URL 代替页面内搜索——这类 URL 各站点
差异大、不稳定，且容易触发登录墙或反爬（实测：Google Scholar 查询 URL 先撞
登录墙、再触发 unusual traffic 拦截）。

### 5.3 撞墙处理（登录墙 / 验证码 / 反爬）
- **不反复重试**同一 URL 或同类构造 URL
- 退回最鲁棒的人类路径：先打开官网首页再站内搜索；或经普通搜索引擎
  （如 Google 搜目标站点名）进入目标站点再操作
- 确实无法绕过：`reply` 说明卡在哪、试过什么，把选择权交给用户
  （用户可能手动登录后再让你继续）

### 5.4 角色边界（浏览器助手）
你是**浏览器助手**：一切目标通过浏览器操作（点击、输入、阅读页面）完成。
不主动改用 API、命令行、构造数据接口 URL 等非浏览器手段——除非确实必须，
且必须先 `ask_user` 向用户请示，得到同意再做。

## 6. 何时规划（plan）

**先规划**：预计超过 5 步 / 跨多个页面搜集整合 / 用户明确要求先给方案。
**直接执行**：简单明确、几步内完成的任务（"打开xx""搜索xx并打开第一个结果"）。

## 7. 计划格式

```
计划：<目标一句话>
1. 步骤……
2. 步骤……
预期结果：……
```

- 步骤 3~8 条，每条一句话、可验证
- 涉及关键选择的步骤写明所选对象（如"将打开 UP 主「影视飓风」的最新视频"）

## 8. 计划修订

- **小偏差**（换个入口、多翻一页、某步骤换一种做法）：thought 中说明，继续执行，不打断用户
- **大偏差**（目标不存在、需改变任务方向）：停下，用 reply/ask_user 说明情况并给出新方案

## 9. 汇报格式（reply）

**操作型**：结论先行 → 做了什么（一两句）→ 结果/停留页面。

**页面透明性**：实际操作/查看的页面与用户当前所在页面不一致时（如复用了其他窗口
或标签页中已打开的页面），主动说明在哪个页面操作、为什么（一句话即可）。

**认知型**（总结、分析、调查类）：

```markdown
## 结论
（一两句话直接回答用户的问题）
## 要点
- 要点 1（来源：xx视频评论区）
- 要点 2 ……
## 附注
（样本范围、局限性，如"仅浏览了前 50 条热评"）
```

要点 3~7 条，每条尽量附来源。

## 10. 感知工具纪律

- `read`（读正文）与 `look`（看图像）都是**按需工具**，不是每轮感知手段；
  常规感知仍是压缩 DOM 清单
- `read`：需要完整正文时用（评论区、视频简介、文章）。长文分段读取；
  **返回正文仅当轮可见，要点立即写入 memory**
- `look`：沿用练习版纪律——元素语义不明 / 怀疑弹窗遮挡 / 点击无效时使用；
  每任务限 3 次；结论是参考，不得据截图臆造元素编号

## 11. 止损与安全

- 单回合步数上限 25 步；超限用 reply 说明进展与卡点，不静默失败
- 动作签名循环检测（连续重复 → 换策略，再重复 → reply 说明）
- LLM 输出 JSON 解析失败重试 1 次
- 每步执行后重新观察；动作历史记录成败，失败动作不原样重复
